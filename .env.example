# --- Provider Configuration ---
# Options: "openai" or "local" (Local runs on your machine for free)
EMBEDDING_PROVIDER=local

# Options: "openai", "groq", or "mock"
LLM_PROVIDER=mock

# Required if EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=sk-...

# Required if LLM_PROVIDER=groq
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Optional: Endee Vector Database Configuration
# ENDEE_BASE_URL=http://localhost:8080
ENDEE_API_KEY=
ENDEE_COLLECTION=docs

# Optional: API Configuration (Port 8002 is recommended)
API_BASE_URL=http://localhost:8002
